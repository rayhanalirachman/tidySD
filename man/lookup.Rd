% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{lookup}
\alias{lookup}
\title{Declare a graphical (lookup) function}
\usage{
lookup(
  name,
  input = NULL,
  in_units = NULL,
  out_units = NULL,
  interp = c("linear", "constant"),
  range = c("clamp", "extend", "na"),
  label = NULL,
  doc = NULL
)
}
\arguments{
\item{name}{Name of the lookup; it is callable on an equation right-hand
side, e.g. `extr_efficiency(Resource)`.}

\item{input}{Name of the variable it is normally read with (documentation
and diagramming only).}

\item{in_units, out_units}{Unit strings for the horizontal and vertical axes.}

\item{interp}{`"linear"` or `"constant"` (step) interpolation.}

\item{range}{`"clamp"` (hold the end values, the Vensim default) or
`"extend"` (linear extrapolation) or `"na"`.}

\item{label}{Optional human-readable label.}

\item{doc}{Optional one-line description, surfaced by [sd_equation_table()].}
}
\value{
An `sd_element` to be passed to [sd_structure()].
}
\description{
The points come from [lookup_data()] in the parameter layer; the structure
layer only says that the function exists, what it reads, and how it behaves
outside the tabulated range.
}
