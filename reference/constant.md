# Constants

Numeric constants available on any equation right-hand side. A value may
be a scalar, a (named) vector over a subscript dimension, or a matrix.

## Usage

``` r
constant(...)
```

## Arguments

- ...:

  Named numeric values.

## Value

An \`sd_element\` to be passed to \[sd_parameters()\].
