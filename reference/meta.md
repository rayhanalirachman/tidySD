# Model metadata

Model metadata

## Usage

``` r
meta(name = NULL, ...)
```

## Arguments

- name:

  Model name, used in printing and as the default plot title.

- ...:

  Further free-form metadata (author, notes, source, ...).

## Value

An \`sd_element\` to be passed to \[sd_structure()\].
