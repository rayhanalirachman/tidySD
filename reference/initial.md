# Stock initial values

Stock initial values

## Usage

``` r
initial(...)
```

## Arguments

- ...:

  Named numeric values, one per stock. A scalar given for a subscripted
  stock is broadcast over every member of its dimension.

## Value

An \`sd_element\` to be passed to \[sd_parameters()\].
